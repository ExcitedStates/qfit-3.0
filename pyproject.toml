[build-system]
requires = [
	"setuptools",  # PEP 508 specifications.
	"wheel",       # PEP 508 specifications.
	"setuptools_scm",
	"numpy>=1.20,<2",
	"cvxpy",
	"pyscipopt",
]
build-backend = "setuptools.build_meta"
