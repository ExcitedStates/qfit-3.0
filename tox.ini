[tox]
requires =
    tox-conda
    numpy
alwayscopy = true

[testenv:with-cplex]
conda_channels =
    ibmdecisionoptimization
    anaconda
    conda-forge
conda_deps =
    tox-conda
    pytest
    numpy
    cplex
    cvxopt
changedir =
    tests
commands =
    pytest --basetemp="{envtmpdir}" {posargs}
